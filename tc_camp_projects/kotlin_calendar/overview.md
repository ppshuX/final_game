# 📊 Kotlin Calendar 项目概览

> 本文档提供项目的整体视图和技术架构

---

## 🎯 项目目标

### 基本要求（作业）

1. ✅ **日历视图展示**（月视图）
2. ✅ **日程增删改查**（CRUD）
3. ✅ **日程提醒功能**

**作业完成度**：100% ✅

### 扩展功能（可选）

- [ ] 周视图/日视图
- [ ] 日程分类/标签
- [ ] 搜索功能
- [ ] 数据导出/备份

---

## 🔧 技术架构

### 架构图

```
┌─────────────────────────────────────┐
│         UI Layer (Activity)         │
│  - MainActivity                     │
│  - CalendarView                     │
│  - RecyclerView + EventAdapter      │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│      Business Logic Layer           │
│  - ReminderManager                  │
│  - Event Validation                 │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│       Data Layer (Room)             │
│  - Event (Entity)                   │
│  - EventDao (DAO)                   │
│  - AppDatabase (Database)           │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│      System Components              │
│  - AlarmManager (定时任务)          │
│  - NotificationManager (通知)       │
│  - BroadcastReceiver (广播)         │
└─────────────────────────────────────┘
```

### 技术栈

| 层次 | 技术选型 | 说明 |
|-----|---------|------|
| **UI** | Material Design | 现代化 UI 风格 |
| **语言** | Kotlin | 简洁、安全 |
| **数据库** | Room (SQLite) | 本地持久化 |
| **异步** | Coroutines | 协程处理 |
| **架构** | MVVM（简化版）| 单 Activity |
| **列表** | RecyclerView | 高性能列表 |
| **定时** | AlarmManager | 系统级定时 |
| **通知** | NotificationManager | 系统通知 |

---

## 📈 项目规模

### 代码统计

- **总文件数**：13 个
- **核心代码**：约 530 行
- **开发用时**：10 小时
- **完成天数**：8 天（跳过 Day 7）

### 文件清单

```
app/src/main/
├── java/com/ncu/kotlincalendar/
│   ├── MainActivity.kt              (200+ 行) - 主程序
│   ├── Event.kt                     (10 行) - 实体类
│   ├── EventDao.kt                  (20 行) - DAO 接口
│   ├── AppDatabase.kt               (30 行) - 数据库单例
│   ├── EventAdapter.kt              (60 行) - RecyclerView 适配器
│   ├── AlarmReceiver.kt             (40 行) - 广播接收器
│   └── ReminderManager.kt           (50 行) - 提醒管理器
├── res/
│   ├── layout/
│   │   ├── activity_main.xml        (50 行) - 主布局
│   │   ├── dialog_add_event.xml     (60 行) - 对话框布局
│   │   └── item_event.xml           (40 行) - 卡片布局
│   └── values/
│       └── colors.xml               (10 行) - 颜色资源
├── AndroidManifest.xml              (20 行) - 应用配置
└── build.gradle.kts                 (20 行) - 依赖配置
```

---

## 🗃️ 数据库设计

### Event 表（v2）

| 字段 | 类型 | 说明 | 备注 |
|-----|------|------|------|
| id | Long | 主键 | 自增 |
| title | String | 标题 | 非空 |
| description | String | 描述 | 可选 |
| dateTime | Long | 日期时间 | 时间戳 |
| createdAt | Long | 创建时间 | 时间戳 |
| reminderMinutes | Int | 提醒时间 | v2 新增 |

**数据库版本**：v2（从 v1 迁移）

---

## 🎨 UI/UX 设计

### 主界面布局

```
┌──────────────────────────────────┐
│  📅 Kotlin Calendar              │
├──────────────────────────────────┤
│                                  │
│   ┌────────────────────────┐    │
│   │   CalendarView         │    │
│   │   (月视图)             │    │
│   └────────────────────────┘    │
│                                  │
│   ┌────────────────────────┐    │
│   │  [+ 添加日程]  button  │    │
│   └────────────────────────┘    │
│                                  │
│   ┌────────────────────────┐    │
│   │ 📋 日程 1              │    │
│   │ 📅 2025-11-05 14:00    │    │
│   └────────────────────────┘    │
│   ┌────────────────────────┐    │
│   │ 📋 日程 2              │    │
│   │ 📅 2025-11-06 10:00    │    │
│   └────────────────────────┘    │
│                                  │
└──────────────────────────────────┘
```

### 交互流程

1. **添加日程**：点击按钮 → 弹出对话框 → 填写信息 → 保存
2. **查看详情**：点击卡片 → 显示详情对话框
3. **编辑日程**：详情对话框 → 点击编辑 → 修改信息 → 保存
4. **删除日程**：长按卡片 → 确认删除 或 详情对话框 → 删除按钮
5. **接收提醒**：系统定时触发 → 显示通知 → 点击消失

---

## 📊 性能优化

### RecyclerView 优化

- ✅ ViewHolder 模式 - 缓存控件引用
- ✅ 复用机制 - 只创建可见项
- ✅ DiffUtil（未实现）- 局部刷新

### 数据库优化

- ✅ 单例模式 - 全局唯一实例
- ✅ 协程异步 - 不阻塞 UI
- ✅ 索引优化（未实现）- 查询加速

### 内存优化

- ✅ 图片资源 - 使用系统图标
- ✅ 对象复用 - ViewHolder 复用
- ✅ 内存泄漏 - lifecycleScope 自动管理

---

## 🔐 权限管理

### 运行时权限

```kotlin
// Android 13+ 需要动态申请
if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.TIRAMISU) {
    requestPermissions(
        arrayOf(Manifest.permission.POST_NOTIFICATIONS),
        REQUEST_CODE
    )
}
```

### 权限列表

- `POST_NOTIFICATIONS` - 发送通知（Android 13+）
- `SCHEDULE_EXACT_ALARM` - 精确定时任务
- `USE_EXACT_ALARM` - 使用精确闹钟

---

## 🐛 常见问题

### 1. 提醒延迟 2-5 分钟？

**原因**：Android 省电机制（Doze Mode）  
**解决**：这是正常现象，符合日历 App 标准

### 2. 通知不显示？

**检查**：
1. 权限是否授予？
2. 通知渠道是否创建？
3. 系统通知是否关闭？

### 3. 数据库迁移失败？

**原因**：版本号未更新或迁移逻辑错误  
**解决**：检查 `@Database(version = X)` 和 `Migration`

---

## 📚 学习资源

- [Kotlin 官方文档](https://kotlinlang.org/docs/)
- [Android 开发者指南](https://developer.android.com/)
- [Room 持久化库](https://developer.android.com/training/data-storage/room)
- [协程指南](https://kotlinlang.org/docs/coroutines-guide.html)

---

## 🔗 相关文档

- [README.md](./README.md) - 项目首页
- [learning_notes.md](./learning_notes.md) - 学习笔记
- [每日日志](./README.md#每日开发日志) - 详细开发记录

